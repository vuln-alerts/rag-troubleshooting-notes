# Retrieval Quality
Retrieval quality problems are not always obvious at first.
Even when retrieval technically works, unrelated or low-quality context can slowly reduce output consistency.

Things worth checking:
* noisy documents
* duplicated references
* unrelated embeddings
* weak filtering logic
* inconsistent chunking structure

Better retrieval quality often improves workflow stability more than increasing model complexity.
